[project]
name = "backend"
version = "0.1.0"
description = "Mars - A minimal code agent powered by OpenCode"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pywebview>=6.1",
    "requests>=2.32.5",
]

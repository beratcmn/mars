[project]
name = "backend"
version = "0.1.0"
description = "Mars - A minimal code agent powered by OpenCode"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pip>=25.3",
    "pywebview>=6.1",
    "requests>=2.32.5",
]

[dependency-groups]
dev = [
    "ruff>=0.14.8",
]
